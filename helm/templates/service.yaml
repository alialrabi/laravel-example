apiVersion: v1
kind: Service
metadata:
  name: {{ include "coverwhale.fullname" . }}
  labels:
    {{- include "coverwhale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coverwhale.selectorLabels" . | nindent 4 }}
